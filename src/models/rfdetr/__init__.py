"""
RF-DETR model wrapper for palynomorph detection.

Modules:
    train   — Fine-tune RF-DETR on a COCO-format tile dataset
    predict — Run inference with a trained RF-DETR checkpoint
    tune    — Bayesian hyperparameter search over RF-DETR training configurations
"""
